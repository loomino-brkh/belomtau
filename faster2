#!/bin/bash

# Get the directory where this script is located
BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Execute the main script with all arguments
exec "$BASE_DIR/scripts/main.sh" "$@"